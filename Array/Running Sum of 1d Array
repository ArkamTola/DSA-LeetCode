class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        int count = 0;
        vector<int> res;
        for(int i = 0; i< nums.size(); i++)
        {
            count = count + nums[i];
            res.push_back(count);
        }
        return res;
    }
};
